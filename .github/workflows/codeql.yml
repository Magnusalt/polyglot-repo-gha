name: "CodeQL C# Analysis"
on:
  push:
    branches:
      - main

jobs:
  call-codeql-csharp:
    uses: Magnusalt/gha-dev/.github/workflows/codeql-extended.yml@v2
    with:
      languages: ["csharp"]